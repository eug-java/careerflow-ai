careerflow:
  jwt:
    secret: ${JWT_SECRET}
  internal-api-key: ${CAREERFLOW_INTERNAL_API_KEY}

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

management:
  endpoint:
    health:
      show-details: when_authorized
